package com.nicolasdeory.syllabler;

import java.util.Arrays;
import java.util.List;

public class Nada {
    // Not practical as it could also mean swimming which is a different word
 /*
    private boolean isNada(List<CharSequence> syll) {
        return (syll.size() == 2 && (syll.get(0).toString().equalsIgnoreCase("na") && syll.get(1).toString().equalsIgnoreCase("da")));
    }

    @Override
    public List<CharSequence> apply(Syllabler word) {

        List<CharSequence> syllables = word.getSyllables();
        if (isMuy(syllables))
            return Arrays.asList("mu");

        return syllables;
    }*/
}
